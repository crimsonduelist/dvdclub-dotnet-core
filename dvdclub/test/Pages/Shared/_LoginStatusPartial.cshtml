@if(User.Identity.IsAuthenticated){
	<form method="post" class = "form-inline" asp-page="/Account/Logout">
		Welcome @User.Identity.Name
		<button type="submit" class="ml-2 btn -link">Logout</button>
	</form>
}else{
	<a class = "btn btn-lin" asp-page="/Account/Login">LoginPartial</a>
}